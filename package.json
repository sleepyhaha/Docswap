{
  "name": "docswap",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "start": "react-dotenv && server/server.js",
    "develop": "concurrently \"react-dotenv && cd server && npm run watch\" \"react-dotenv && cd client && npm run dev\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "seed": "cd server && npm run seed",
    "build": "react-dotenv && cd client && npm run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "react-dotenv": "^0.1.3"
  },
  "react-dotenv": {
    "whitelist": [
      "UC_KEY"
    ]
  }
}
