{
  "name": "docswap",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "start": "react-dotenv && server/server.js",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && react-dotenv && npm run dev\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "seed": "cd server && npm run seed",
    "build": "cd client && npm run build && react-dotenv"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "react-dotenv": "^0.1.3"
  },
  "react-dotenv": {
    "whitelist": [
      "UC_KEY"
    ]
  }
}
